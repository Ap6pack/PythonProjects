# Spatial primitives engine (Phase 1) + API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
shapely>=2.0
pyproj>=3.6

# AI orchestration (Phase 2) — LLM function calling
anthropic>=0.117

# Testing
pytest>=8.0
httpx>=0.27

# PostGIS-backed data layer (Phase 0) — used by the data pipeline and, later,
# by the primitives when they run against the database instead of in-memory.
psycopg[binary]>=3.1
geopandas>=1.0
